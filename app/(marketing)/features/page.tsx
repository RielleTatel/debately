export default function FeaturesPage() {
  return (
    <div className="mx-auto max-w-4xl px-6 py-24 lg:px-8">
      <p className="text-xs font-medium uppercase tracking-widest text-blue-700">
        Features
      </p>
      <h1 className="mt-3 text-4xl font-semibold tracking-tight text-slate-900 md:text-5xl">
        Everything a tournament organizer needs to run administration cleanly.
      </h1>
      <p className="mt-6 max-w-2xl text-lg leading-relaxed text-slate-600">
        A full feature tour is on the way. In the meantime, the highlights on
        the home page cover import, portals, finance, requests, and the audit
        log &mdash; the pieces most directors ask about.
      </p>
    </div>
  )
}
